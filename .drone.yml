kind: pipeline
name: default
type: ssh

server:
  host: yasinshaw.com
  user: root
  ssh_key:
    from_secret: ssh_key

steps:
  - name: build
    image: openjdk:11
    commands:
      - chmod +x ./gradlew
      - ./gradlew clean build